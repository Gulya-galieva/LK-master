@model IEnumerable<DbManager.ReportComment>
@{
    int count = 1;
}
@foreach (DbManager.ReportComment item in Model.Reverse())
{
    @*<div class="media mb-2 comment">
            <div class="media-body">
                <div class="d-flex">

                        <h5 class="mt-0 ml-1">@item.User.Name</h5>

                    <div class="ml-auto">
                        <span class="badge badge-pill badge-primary">@item.Date.ToShortDateString()</span>
                    </div>
                </div>

                <span class="ml-2">@item.Text</span>
            </div>
        </div>*@

<blockquote class="blockquote">
    <div class="d-flex">
        <div class="mr-4">
            <span class="comment-icon">
                <i class="fas fa-quote-right"></i>
            </span>
        </div>
        <div class="mt-2">
            <p class="mb-0">@item.Text</p>
            <footer class="blockquote-footer">@item.User.Name <cite title="Source Title">@item.Date.ToShortDateString()</cite></footer>
        </div>
    </div>
</blockquote>
}


